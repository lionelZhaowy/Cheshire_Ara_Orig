# 本工程的 Agent 协作入口

本文件使用工具通常识别的标准名称 `AGENTS.md`，作用域为当前仓库。它是工作约定，不是执行全部待办的授权。

## 新对话如何开始

1. 先读 [项目状态](docs_codex/PROJECT_STATE.md)，了解目标、已完成与未验证事项。
2. 再读 [任务分工](docs_codex/AGENT_TASKS.md) 中与本次用户请求对应的任务，以及该任务最新交接记录。
3. 只读该任务需要的专题文档和源码；不要默认通读全部教程或重新扫描全仓库。
4. 执行前检查 `git status --short`、当前提交及涉及文件。文档记录是快照，不替代当前源码。
5. 用户只要求解释、诊断或写文档时，不自动改 RTL、运行大型构建或实施迁移。

## 项目方向

- 基于 Cheshire + CVA6 + Ara，面向加入 LVDS 图像接口、ISP、脉动阵列/NPU、外购 AXI4 DDR 子系统，最终经过 IC 验证及物理实现流片。
- 团队有 MIPS/自定义总线经验，正在学习 RISC-V/AXI；回答用中文，按问题逐步解释。
- 用户已在 VCU118 跑通 HelloWorld；不把这一结果扩大解释成 Ara、DDR 全功能或 ASIC 就绪。
- 用户不希望投入大量时间维护 Bender。迁移目标是独立 `cheshire_ara_asic/`、固定配置和静态清单；Bender 至多用于一次性离线解析。
- 详细提取规格见 [ASIC 提取交接书](docs_codex/02_Cheshire_Ara_ASIC_Extraction_Handoff.md)。

## 保护与边界

- 保留现有可运行 FPGA 工程。没有本次明确授权，不覆盖旧编译脚本、不升级依赖、不批量裁切模块。
- `.bender/` 包含已纳入版本管理的依赖源码和本地修改，不是可删除缓存。禁止把 `bender clean`、删除 `.bender` 或重拉上游作为常规修复。
- 保留第三方版权、许可证、源路径/版本和本地差异。现有未提交/未跟踪文件也可能属于用户或其他对话，不能覆盖或清理。
- 不根据 Vivado 文件列表就认为 ASIC 源码可用；XPM/MIG/MMCM/PAD 等需独立适配。
- 禁止把已有 ELF、bitstream、静态阅读或通过编译写成“已重新仿真/已板测/可流片”。报告具体证据和缺口。
- 不在文档中保存供应商受限交付内容、许可证密钥或访问凭据；只记录授权范围内的接口摘要和受控资料位置。

## 多对话协作

- 分工面向用户手动打开的不同对话，不要求自动启动子 Agent，也不自动授予其他任务的实施权限。
- 每个实施对话明确任务 ID、输入版本、负责文件、允许修改范围和交付物。
- 优先由用户安排不同 worktree/分支；同一工作区并行时避免修改同一文件。任务表不是文件锁，不能保证互斥。
- `hw/cheshire_pkg.sv`、`hw/cheshire_soc.sv`、固定配置包、filelist、地址图、链接脚本和共享状态文档由同一时段的一位协调者汇总修改。其他对话先提交独立交接建议。
- 不能靠覆盖别人的文件、`git reset --hard`、切换分支或批量还原来解决冲突；先核对并协调。
- 每个有实质进展的任务结束时，在 `docs_codex/handoffs/` 写独立记录，使用 [交接模板](docs_codex/HANDOFF_TEMPLATE.md)。记录命令、结果、文件、未验证项和最小下一步。
- 项目事实变化时更新 `PROJECT_STATE.md`；任务进展变化时更新 `AGENT_TASKS.md`。若有人同时编辑这两个文件，先只写自己的交接记录，由协调者合并。

## 阅读与冲突处理

- 当前用户明确要求决定任务范围；近期确认的需求取代旧规划。不能把其他 Agent 的建议自动升级为用户决策。
- 实现事实以当前源码和可追溯测试为准；文档冲突时指出差异并记录证据，不静默选择有利结论。
- `00_README.md`、`01_Cheshire_Ara_Quick_Start.md` 保留历史分析；其中 AXI3 DDR、旧工作区状态和 Bender 长期管理建议不代表当前方向。
- 不把整个聊天逐字搬进共享文档。共享状态保持简短，细节放专题与独立交接记录。
