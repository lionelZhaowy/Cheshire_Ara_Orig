# 项目共享状态

最后整理：2026-09-22。维护者：后续由用户指定的协调对话；各任务通过独立交接记录提供更新。

本文件是新对话的简短上下文，不是测试报告。需求变化看用户确认，源码变化看当前工作区，详细进展看 [交接目录](handoffs/README.md)。

## 1. 最终目标与近期目标

最终目标：以 Cheshire + CVA6 + Ara 为起点，集成 LVDS 图像输入、ISP、脉动阵列/NPU、采购的 DDR 控制器及相关 IP，裁切无用模块，迁移到团队 EDA 服务器，完成 IC 设计、验证、DFT 与物理实现并流片。

团队背景：有 MIPS 处理器及自定义总线经验，RISC-V/标准 AXI 的软硬件经验尚在积累；项目时间紧，希望分阶段学习、直接推进必要工作。

近期目标：把正确的向量组合提取为能独立搬迁的 `cheshire_ara_asic/`。只维护一个目标配置、静态源码清单和团队自己的脚本，不继续把 Bender 工程管理当作主要任务。

本轮（2026-09-22）用户优先要求完成学习教材；工程提取、RTL/IP 修改和大型仿真不在本轮范围。

当前提取状态：**尚未实施；整理时该目录不存在。** 已建立协作/交接文档，并于 2026-09-20 完成 L01 配置参考手册；均未实施 RTL 提取。

## 2. 需求、建议和未决项

| 性质 | 内容 |
| --- | --- |
| 用户确认 | DDR 供应商可以提供 AXI4 版本，不再把 AXI3 桥列为必选项 |
| 用户要求 | NPU/ISP 数据通路需要绕过 LLC 直接访问 DDR，避免其流量污染 CPU 的 LLC |
| 用户要求 | 提取独立 `cheshire_ara_asic`；不在 Bender 和旧脚本体系上投入过多精力 |
| 用户要求 | 使用多个独立对话分工，交接信息保存在仓库中 |
| 提取执行建议 | `cv64a6_imafdcv_sv39` + Ara 2 lanes / VLEN 2048，以当前 Ara 仿真配置取值快照作为首个固定基线；见 02 文档 |
| 架构建议，尚未实现 | DMA 共享数据区采用全路径不缓存或经验证的缓存维护，加缓冲区所有权和 MMIO/中断交接 |
| 未决 | 最终外设保留表、地址分配、DMA 区大小、最终性能/功耗/面积指标 |
| 未决 | 工艺、SRAM 宏、PLL/PAD、DDR 控制器/PHY 细节、DFT/MBIST 和 EDA 工具版本/许可 |
| 未决 | 最终启动介质、操作系统需求、是否需要跨 CPU/设备的共享原子控制数据 |

提取基线不是最终产品架构冻结；不要擅自替用户完成未决选择。

## 3. 已完成事项及证据等级

| 事项 | 状态及证据边界 |
| --- | --- |
| GitHub 工程下载、本地 ModelSim 仿真、Vivado 工程重建、VCU118 HelloWorld 串口输出 | **用户报告已完成**；本对话未重新执行这些流程 |
| 依赖源码与本地修改保存 | 根提交 `379ae4544bc62e05a2736b11b33a3244181bba38` 包含 vendor 快照；`Bender.local` 使用本地相对路径 |
| 依赖可搬迁性历史检查 | `.bender/README.md` 记载异路径离线解析/清单检查；不是本轮重新编译或仿真 |
| 系统入门、配置、启动、LLC/SPM、Ara 路径分析 | 已写入 00/01 文档，并在后续对话中澄清；主要为源码检查 |
| Ara 配置核查 | 现有 FPGA 加源脚本使用向量 profile；现有仿真脚本仍为标量 profile，且同时包含 decoder stub 与真实实现 |
| 本地向量软件产物检查 | `fmatmul_test_4x4.spm.elf` 反汇编可见 RVV 指令；不代表当前板上执行该产物或结果已通过 |
| ASIC 固定配置提取方案 | 02 文档已创建；**源码提取、编译、仿真尚未完成** |
| 多对话协作入口 | 根 `AGENTS.md`、本状态页、任务表和交接模板已创建；未自动启动其他任务 |
| L01 配置参考手册（2026-09-20） | [六篇配置文档](configuration/README.md)，覆盖 Cheshire 113 个字段、CVA6 88 个用户字段及 Ara/外设/常用配置；本轮仅源码与文档静态核对，未编译/仿真/板测；[交接](handoffs/2026-09-20_L01_configuration_reference.md)记录参数消费者差异与待验证项 |
| L01 软件学习手册（2026-09-20） | [五篇软件文档](software/README.md)：SDK/工具链、编译链接、启动调试、库API和示例流程；覆盖根tests 9个主源及Ara 18个应用/测试源，命令仅静态/语法检查；[交接](handoffs/2026-09-20_L01_software_guide.md)记录环境与待核查项 |
| L01 离线入门教材（2026-09-22） | [网站入口](learning/index.html)：10 页、10 张 SVG、8 个递进实验；教材内 8 次轻量软件构建及网页/浏览器检查通过，独立导出向量仿真清单；**未 RTL 编译展开/仿真/板测**。工具查询为 GCC 15.2.0、Questa 2024.1，VCS/vlogan 不在 PATH；[交接](handoffs/2026-09-22_L01_learning_site.md)记录告警与缺口 |
| L01 进阶扩充（2026-09-22） | [进阶入口](learning/advanced.html)：新增14页/8图，全站24页/18图，覆盖全部顶层外设、寄存器/驱动/RTL、时钟复位、电源边界、CVA6/Ara内部与综合案例；429项离线寄存器索引。8个新软件版本构建、12个人工日志检查、浏览器桌面/手机预览通过；**未运行RTL**。[独立交接](handoffs/2026-09-22_L01_advanced_peripherals.md)记录iDMA位宽缺口、HAL审读问题和各外设验证条件 |

整理时根分支为 `mp/ara-pulp-v2`，HEAD 如上。01 中提到的 `e040b7bbc` 和“10 个未提交修改”属于历史快照。执行任务前重新检查提交和工作区，不能把本段当作实时 Git 状态。

## 4. 必须共享的技术事实

1. **两层硬件配置不自动联动。** 根 `cheshire.mk` 默认选择不带 V 的 `cv64a6_imafdchsclic_sv39_wb`；`SELCFG=3` 只选择开启 Ara 的 SoC 参数，不能改掉已选 CPU profile。正确向量 profile 是 `cv64a6_imafdcv_sv39`，并排除 CVA6 first-pass decoder stub。
2. **FPGA 配置不等于仿真配置。** `add_sources.vcu118.tcl` 已选向量 profile/ARA/2 lanes/2048；但生成文件正确不证明当前 bitstream 与全部源码一一对应。FPGA 清单也不能直接作为 ASIC 清单。
3. **软件要单独核对。** 根软件默认 ISA 不带 V；Ara 软件入口会加 V。程序需在首条向量指令前启用 VS，浮点还涉及 FS。HelloWorld 设置 VS 并不证明执行了向量计算。
4. **Ara 接口不要按名字误判。** 当前 `gen_cva6_cfg()` 设置 `CvxifEn=0`；相关 `cvxif` 端口名在此组合承载专用加速器/MMU 交互，不是可任意替换的通用插槽。
5. **SPM 使用 LLC 存储阵列。** 默认 128 KiB，Boot ROM 等待自测并配置为全 SPM 后设置栈。可按 way 在运行时切换/混合，但需管理栈、数据、事务和 cache flush。移除 LLC 会同时影响启动片上存储。
6. **ATOP 是 AXI4 之外、来自 AXI5 的原子事务扩展。** 当前主存路径在 LLC 前处理原子操作，下游 AXI4 DDR 无需因此自行实现 AMO。不要用返回错误的 ATOP filter 替代原子执行器。
7. **设备旁路可行，但不是自动一致。** 旁路写不会自动被上游原子/LRSC 监视器看到；CPU/Ara/LLC 缓存副本也可能过期。禁止把“不发 AMO”推导成“普通写不影响原子性”。将原子单元移到 LLC 后面或新增 DDR 互连都不会自动解决这些问题。
8. **裸机也可以使用 GPT。** 本地通用 GPT 镜像规则已注释，Boot ROM 的 GPT/raw 启动解析代码仍在；直接加载 ELF 通常不依赖 GPT。镜像打包停用不等于硬件启动能力被删除。
9. **依赖快照不是完整 SDK 离线镜像。** 可选 benchmark、未初始化子仓库等排除项见 `.bender/README.md`；不要声称所有软件环境都完整。
10. **iDMA控制接口增加运行门槛（L01进阶静态复核）。** `cheshire_idma_wrap`使用64-bit Regbus，`idma_reg64_2d_reg_top`固定DW=32，旧桥未显示lane搬移；原HAL对+0x44/+0x84的32位ID用64位指针，GCC实测拆成两次32位读。需S01/V01独立统一并验收；教材capstone默认BLOCKED/rc16，不表示DMA通过，未改RTL/sw。该wrapper没有DMA完成IRQ。
11. **外设RTL存在不等于当前平台可验证。** fixture的GPIO输入绑零、VGA输出悬空、USB PHY时钟绑零；VCU118未启用I2C/SD/VGA/USB引脚功能，Serial Link未接出。Link隔离/门控输出在SoC悬空，不能据寄存器名推断实际隔离或电源域。详见进阶支持矩阵和时钟图。

## 5. 近期待办及依赖关系

1. **E01 源码提取**：按 02 文档实现固定配置、静态清单和独立 wrapper。
2. **V01 验证、S01 软件**：可先制定测试/软件计划；对新目录的实现与回归依赖 E01 确定的目录和接口。
3. **M01 DDR/共享存储**：可立即做只读分析与接口契约，实际修改拓扑需另行授权并基于提取基线。
4. **I01 图像/NPU、P01 工艺**：先明确外部规格和需求；不要在未有资料时虚构 IP 实现。
5. 外设裁切在最小回归后进行，并先确认产品保留表。Linux/RTOS 不作为当前裸机提取的强制前置。

任务定义、负责文件及可复制的新对话提示词见 [AGENT_TASKS.md](AGENT_TASKS.md)。

## 6. 文档导航和更新规则

- [根协作约定](../AGENTS.md)：所有对话的通用边界。
- [任务表](AGENT_TASKS.md)：分工、状态和启动方式。
- [提取交接书](02_Cheshire_Ara_ASIC_Extraction_Handoff.md)：E01 的完整规格；第 8 节也是 M01 输入。
- [入门教程](01_Cheshire_Ara_Quick_Start.md)：只按任务读取相关章节；不是实时进度表。
- [离线学习教材](learning/index.html)：L01 中文教学主线、独立 C/RVV 示例、仿真实验命令和证据；维护见 [README](learning/README.md)。
- [配置参数手册](configuration/README.md)：L01 当前源码配置字典、实际覆盖关系、常用配置与修改检查。
- [软件学习手册](software/README.md)：SDK/工具链、编译链接、启动调试、嵌入式库与示例流程。
- [交接模板](HANDOFF_TEMPLATE.md) 与 [交接索引](handoffs/README.md)：保存每次实际结果。

重大更新附日期、任务 ID 和证据记录链接。保留“用户报告 / 源码确认 / 已有产物 / 本轮实测 / 方案建议”区别。不在本页堆积终端日志或聊天全文；也不为没有完成的任务写“完成”。
