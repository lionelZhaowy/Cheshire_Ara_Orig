# C00 初始交接：从单一长对话转为分任务协作

日期：2026-09-20。状态：协作文档初始化完成；没有启动 RTL 提取或其他实施任务。

## 用户已明确的信息

- 已完成本地下载、ModelSim 仿真、Vivado 重建、VCU118 HelloWorld 输出；由用户报告，不是本轮复测。
- 目标为 Cheshire/CVA6/Ara + LVDS/ISP/脉动阵列/NPU + 外购 DDR，最终流片。
- DDR 供应商可提供 AXI4；NPU/ISP 数据通路希望绕过 LLC。
- 希望提取独立 `cheshire_ara_asic/`，不在 Bender 管理和旧脚本上浪费时间。
- 本轮只要求建立多对话共享文件，不授权顺便实现上述全部功能。

## 上一段讨论形成的结果

已说明两层硬件配置、Ara 使用条件、LLC/SPM 共享及运行时切换、GPT 镜像的作用；本地证据摘要见 [项目状态](../PROJECT_STATE.md)，详细提取要求见 [02 文档](../02_Cheshire_Ara_ASIC_Extraction_Handoff.md)。

关于旁路的最终结论：允许 NPU/ISP 通过新的 DDR 侧 AXI 互连绕过 LLC；普通 DMA 无需支持 AMO，但共享缓冲区的缓存可见性和跨路径原子性必须另行设计。推荐所有权交接/不缓存 DMA 区/MMIO 通知，尚未实现或作为最终产品规格冻结。

之前“不要绕过共享路径”的笼统说法已收窄为上述条件，不再把旁路本身视为禁止项。也不要求 DDR 控制器因当前 CPU 原子指令而必须支持 AWATOP。

## 工作区与文档变更

本轮开始时 HEAD 为 `379ae4544bc62e05a2736b11b33a3244181bba38`；02 提取交接书已存在且未跟踪。没有 `cheshire_ara_asic/`。

本轮新增根 `AGENTS.md`、项目状态、任务表、交接模板及本交接目录；更新 00/01 的导航/历史说明，避免新对话继续把旧 AXI3 规划和 Bender 维护建议当作当前要求。

没有修改 RTL、软件、依赖或旧构建脚本；没有执行仿真、综合、烧录、依赖下载或 Git 提交。文档未提交状态以接手时 `git status` 为准。

## 验证与限制

本轮检查文档本地链接、改动范围和空白格式。未对新文档中的未来实施命令进行功能验收；不要据此填写 E01/V01 已完成。

此前对 `sw/tests/fmatmul_test_4x4.spm.elf` 做过只读反汇编，确认有 RVV 指令；没有执行该程序，也没有建立 ELF 与当前板上 bitstream 的对应关系。

## 下一位最小继续步骤

1. 读取根 `AGENTS.md`、共享状态和用户指定任务；核对当前工作区。
2. 若用户授权 E01，按 02 文档提取；不先修补全部旧脚本、不同时改变 DDR 架构。
3. M01/P01/I01 可以先独立分析和收集接口规格；各写自己的交接，避免共同编辑 top/config。
4. 实施结束记录真实测试证据，再由 C00 汇总状态。
